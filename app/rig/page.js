// app/rig/page.js
// There is no rig page any more.
//
// It was several hundred words about why one person's headphones matter,
// shipped inside everybody's software, on a page you had to leave the card to
// read. The setup lives on the card now — press the mark and it comes up from
// the bottom — and what it says is what each thing is and what it does. The
// rest was the journal's job all along.
//
// This stays as a forwarding address rather than a deletion: the card linked
// here for a while, and a link that has been out in the world should land
// somewhere.

import { redirect } from 'next/navigation';

export default function RigPage() {
  redirect('/');
}
